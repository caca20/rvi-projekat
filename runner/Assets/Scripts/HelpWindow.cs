using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HelpWindow : PauseScript
{
    // Start is called before the first frame update
    void Start()
    {
        Started();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
